PORT=4000
DATABASE_URL=postgres://postgres:postgres@localhost:5432/rideshare_connect
ALLOWED_ORIGIN=*
AUTH_EXPOSE_DEV_OTP=true
AUTH_OTP_TTL_MINUTES=5
AUTH_SESSION_DAYS=30

# Leave DATABASE_URL empty to run the API against the seeded corridor candidates
# until PostgreSQL is installed and initialized locally.
